import styled from 'styled-components';

const PageTitle = styled.h1`
  font-size: 4rem;
  margin-bottom: 3rem;
  color: #fff;
  padding-right: 10%;
  letter-spacing: -2px;
  font-weight: normal;
`;

export default PageTitle;
